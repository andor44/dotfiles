[user]
	name = Andor Uhlár
	email = andor44@gmail.com
[alias]
	lg = log --color --graph --decorate --abbrev-commit --all --pretty=format:'%Cred%h%Creset -%C(auto)%d%Creset %s %Cgreen(%cr)%C(bold blue) <%an>%Creset'

[pull]
	rebase = true
[grep]
	lineNumber = true
[core]
	excludesfile = $HOME/.gitignore_global

[url "git@github.com:"]
	insteadOf = https://github.com/
[push]
	default = current

[pager]
	diff = delta
	log = delta
	reflog = delta
	show = delta
